**/node_modules
.git

**/dist
**/build
**/.next
**/.nuxt
**/.output
**/.svelte-kit
**/.astro
**/.turbo
.turbo

**/.wrangler
**/.alchemy
**/.expo
**/.vercel
*.log

Dockerfile
**/Dockerfile
docker-compose.yml

# Secrets stay out of image layers; runtime env comes from compose env_file,
# build-time public values come from compose build args
**/.env
**/.env.*
!**/.env.example

local.db
local.db-*
.data

# Design source files: 26MB of standalone HTML that no image needs. The tokens
# extracted from them are already in source.
designs/

# expo prebuild output. Native build artefacts, regenerated on demand, and
# large enough to dominate the build context if they leak in.
apps/native/android/
apps/native/ios/
